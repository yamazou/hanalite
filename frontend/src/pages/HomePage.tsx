/** Empty shell at startup; replace with dashboard later. */
export function HomePage() {
  return (
    <div className="erp-panel erp-panel-grow">
      <div className="erp-panel-content">
        <p className="muted erp-grid-empty">Select a menu item to open a page.</p>
      </div>
    </div>
  )
}
